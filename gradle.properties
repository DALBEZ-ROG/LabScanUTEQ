# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# When enabled, the Configuration Cache allows Gradle to skip the configuration
# phase entirely if nothing that affects the build configuration (such as build scripts)
# has changed. Additionally, Gradle applies performance optimizations to task execution.
org.gradle.configuration-cache=true
# AGP 9 aplica Kotlin por su cuenta (built-in Kotlin). No se aplica el plugin
# org.jetbrains.kotlin.android: es incompatible con el DSL nuevo de AGP 9.

# Compose necesita AndroidX.
android.useAndroidX=true

# IP LAN del PC que corre el backend RAG. Es solo el valor POR DEFECTO del APK: la
# pantalla de Ajustes de la app lo sobrescribe en caliente, sin recompilar. Desde
# 2026-09-09 el network_security_config de depuracion permite cualquier host, asi que
# cambiar de red ya no obliga a tocar este archivo (ver D-034).
labscan.devHost=172.20.135.199

# El backend simulado de F5. Ya no hace falta: el backend real existe y responde.
# Ponlo en true para volver a desarrollar sin backend.
labscan.useMock=false
